<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enter OTP</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f9f9f9; /* Light background color */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .otp-container {
            max-width: 400px;
            width: 100%;
            padding: 30px;
            background-color: #ffffff; /* White background for the form */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .otp-header {
            margin-bottom: 20px;
            font-size: 1.75rem;
            font-weight: 600;
            color: #333;
            text-align: center;
        }
        .form-label {
            color: #555; /* Slightly darker label color */
        }
        .form-control {
            text-align: center;
            font-size: 1.5rem; /* Larger font size for OTP input */
            letter-spacing: 0.1em; /* Spacing between characters */
        }
        .btn-primary {
            background-color: #4a90e2;
            border-color: #4a90e2;
            border-radius: 5px;
            padding: 10px;
            font-size: 1rem;
            margin-left:150px;
            margin-top:10px;
        }
        .btn-primary:hover {
            background-color: #357abd;
            border-color: #357abd;
        }
        .text-center {
            text-align: center;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="otp-container">
        <h2 class="otp-header">Enter OTP</h2>
        <form action="/verifyotpss" method="POST">
            <div class="mb-3">
                <label for="otp" class="form-label">OTP:</label>
                <input type="text" id="otp" name="otp" class="form-control" placeholder="Enter your 4-digit OTP" maxlength="4" required  autofocus>
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.min.js"></script>
</body>
</html>
